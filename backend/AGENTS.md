# AGENTS.md

Before running commands, first check whether the backend is already running in Docker.

If a backend container is already running, prefer executing commands inside that container rather than on the host.

Default mindset: container-first, not host-first.